import React from "react";
export const metadata = {
  title: "About",
  description: "Nextly About Section",
};

export default function AboutLayout({ children }) {
  // return <div>{children}</div>;
  return <>{children}</>;
}
